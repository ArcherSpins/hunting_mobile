const initialState = {
    hunting_form: [],
    loading_default_hunting_farm: true,

    errorHuntingFarmDefault: {
        errorLoaded: false,
        message: null
    },
}

export default (state = initialState, action) => {
    switch(action.type) {
        
    }
}